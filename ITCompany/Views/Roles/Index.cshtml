@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityRole>

<div class="container py-5">    <div class="card mb-4 box" id="tables">
        <a asp-area="" asp-controller="Account" asp-action="Index">
            <button type="button" class="btn border-black">
                <strong>Вернуться на панель администратора</strong>
            </button>
        </a>
        <h3 class="section-title">Список ролей</h3>
        <div class="card-body">
            <table class="table table-hover">
                <tbody>
                    @foreach (var role in Model)
                    {
                        <tr>
                            <td><h4>@role.Name</h4></td>
                            <td>
                                <form asp-area="Admin" asp-action="Delete" asp-route-id="@role.Id" method="post">
                                    <button type="submit" class="btn btn-red btn-danger">
                                        Удалить
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            <a asp-area="Admin" asp-controller="Roles" asp-action="Create">
                <button type="button" class="btn border-black">
                    <strong>Добавить роль</strong>
                </button>
            </a>
            <a asp-area="Admin" asp-controller="Roles" asp-action="UserList">
                <button type="button" class="btn border-black">
                    <strong>Список пользователей</strong>
                </button>
            </a>
        </div>
    </div>
</div>